{% extends 'Base/base.html.twig' %}

{% block corpo %}
    <table class="table">
        <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Nome vendedor</th>
            <th scope="col">Email</th>
        </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ vendedor.id|default('-') }}</td>
                <td>{{ vendedor.nome|default('-') }}</td>
                <td>{{ vendedor.email|default('-') }}</td>
            </tr>
        </tbody>
    </table>
    <br>
    <table class="table">
        <thead>
        <tr>
            <th scope="col">Valor da venda</th>
            <th scope="col">Comissão do vendedor</th>
            <th scope="col">data da venda</th>
        </tr>
        </thead>
        <tbody>
        {% for venda in vendedor.vendas|default([]) %}
            <tr>
                <td>{{ venda.valor_venda }}</td>
                <td>{{ venda.comissao }}</td>
                <td>{{ venda.data_venda }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}